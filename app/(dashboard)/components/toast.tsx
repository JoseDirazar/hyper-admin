'use client'
import { toast } from "react-hot-toast";
export const Buttons = () => {
    return (
        <button onClick={() => toast.success('trololololo lo lo lo lolo, trololololo')}>El queee?</button>
    )
}